---
"lovstudio": patch
---

Automatically label the current tmux pane while `lovstudio app` runs, keep every pane title visibly labeled without focusing it, and restore the previous title on exit or interruption.
